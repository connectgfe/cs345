/* Project 2 utility class:
 *
 * Represents the pairing of a key (an int) with a value (a String).  We will
 * sort these objects, but the sorting will always (and only) be by the key,
 * ignoring the value.
 */

public class Proj02_DataPair
{
	public int    key;
	public String value;
}

